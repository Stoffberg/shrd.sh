# PUNCHLIST

Ordered by impact on a cold senior-engineer review. An item is closed only after the fix is verified and committed.

## Open

No open items.

## Closed

1. [x] README is stale and undersells the project. It documented removed AI config, SDK metadata/delete methods, stats endpoints, web-style HTML negotiation, and old defaults. Replaced it with a current product README covering what/why, terminal demo, architecture, setup, API, SDK, self-hosting, and tech decisions. Verified with `pnpm install --frozen-lockfile`, `cargo build --manifest-path cli/Cargo.toml --release`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
2. [x] Rust lint is not green. `cargo clippy --manifest-path cli/Cargo.toml --locked --all-targets -- -D warnings` failed on two `unnecessary_sort_by` findings in multipart upload code. Replaced both manifest part sorts with `sort_by_key`. Verified with `cargo fmt --manifest-path cli/Cargo.toml -- --check`, `cargo clippy --manifest-path cli/Cargo.toml --locked --all-targets -- -D warnings`, and `pnpm ci:full`.
3. [x] Root `pnpm lint` is a fake green check. It exited 0 while Turbo ran zero lint tasks. Replaced it with `lint:ts` plus `lint:rust`, removed the unused Turbo lint task, and wired `ci:core` through `pnpm lint`. Verified with `pnpm lint` and `pnpm ci:full`.
4. [x] The dead web app remained in the workspace. `pnpm build` scoped `@shrd/web` and replayed a giant TanStack/Shiki server bundle. Deleted `apps/web`, narrowed the workspace to `apps/api`, and regenerated the lockfile. Root `pnpm build` now scopes `@shrd/api`, `@shrd/db`, `@shrd/sdk`, and `@shrd/shared` only, dropping from 5 scoped packages and 3 build tasks to 4 scoped packages and 2 build tasks. Verified with `pnpm install --frozen-lockfile`, `pnpm build`, and `pnpm ci:full`.
5. [x] Scaffolded auth and account schema remained despite the no-account product contract. Deleted the unused Better Auth module, removed `better-auth` and `@atinux/kysely-d1`, and trimmed account/session/user/api-key/collection tables plus `shares.user_id` from the Drizzle schema and migration snapshot. Verified no non-multipart auth/account tables remain with `rg`, then ran `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
6. [x] Public docs beyond README were stale. Deleted obsolete `PLAN.md` and `SELF_HOSTING.md`, replaced `CONTRIBUTING.md` with the current setup/checks/layout, refreshed `CHANGELOG.md`, and updated `AGENTS.md` after the web workspace deletion. Verified stale web/auth/default-expiry/integration-script references with `rg`, then ran `pnpm build` and `pnpm ci:full`.
7. [x] API metrics code still carried removed browser/stats concepts. Deleted `readsHtml`, public stats aggregation helpers, content-type rollups, storage snapshots, and scheduled snapshot writes while keeping core internal upload/read/error counters. Worker dry-run size dropped from 155.52 KiB / gzip 33.57 KiB to 151.00 KiB / gzip 32.76 KiB. Verified removed symbols with `rg`, then ran `pnpm build`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
8. [x] CLI implementation was a 2,474-line `main.rs`. Split behavior into focused modules for app dispatch, crypto, share references, support helpers, upload, download, output, and tests. `main.rs` is now 482 lines and the largest implementation files are 561 lines. Verified with `cargo check --manifest-path cli/Cargo.toml --locked`, `cargo fmt --manifest-path cli/Cargo.toml -- --check`, `cargo clippy --manifest-path cli/Cargo.toml --locked --all-targets -- -D warnings`, `cargo test --manifest-path cli/Cargo.toml --locked`, `pnpm build`, and `pnpm ci:full`.
9. [x] Release metadata was inconsistent. Aligned root, API, SDK, DB, shared package manifests, and `cli/Cargo.toml` on `0.1.12`; added `scripts/check-release-versions.mjs`; wired `pnpm release:check` into `ci:core`; and added the same metadata check to the CLI release workflow before artifact builds. Verified with `pnpm release:check`, `pnpm install --frozen-lockfile`, `pnpm build`, and `pnpm ci:full`.
10. [x] Review pass found README self-hosting told users to run `pnpm --filter @shrd/db generate` before applying migrations. Removed that step so self-hosting applies the checked-in migration set from the Wrangler `migrations_dir`. Verified with `rg`, `pnpm build`, and `pnpm ci:full`.
11. [x] Review pass cold clone failed at `pnpm install --frozen-lockfile` on Node 26 because Wrangler's Sharp dependency fell back to source build without `node-addon-api`. Configured pnpm to skip the unused Sharp native build. Verified in a clean copy with `pnpm install --frozen-lockfile`, `pnpm build`, `cargo build --manifest-path cli/Cargo.toml --release`, `./cli/target/release/shrd --help | sed -n '1,80p'`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
12. [x] Review pass found one inline comment in shared content detection code despite the repo no-comment rule. Removed it. Verified with `rg`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
13. [x] Multipart completion could finalize incomplete uploads because the API did not verify contiguous uploaded parts or stored part sizes against `X-Total-Size`. Added completion validation and regression coverage for size mismatches and missing earlier parts. Verified with `pnpm --filter @shrd/api test:unit`, comment-policy `rg`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`. Worker dry-run moved from 151.00 KiB / gzip 32.76 KiB to 152.13 KiB / gzip 33.00 KiB.
14. [x] `.env.example` still advertised removed OAuth and Better Auth variables despite the no-account product contract. Trimmed it to the actual `SHRD_BASE_URL` override. Verified with stale auth `rg`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
15. [x] CI release compatibility checked only two Linux targets while the release workflow built six platform targets, and `actionlint` reported shell hygiene issues in the release workflow. Expanded release compatibility to the full release target matrix and cleaned shell quoting/globs/output writes. Verified with `actionlint .github/workflows/ci.yml .github/workflows/deploy.yml .github/workflows/release-cli.yml`, `cargo check --manifest-path cli/Cargo.toml --locked --release --target aarch64-apple-darwin`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
16. [x] CLI release metadata still pointed at the stale repository `https://github.com/stoff/shrd`, and the release check only verified versions so it missed the drift. Updated `cli/Cargo.toml` to the actual `Stoffberg/shrd.sh` repository and expanded `pnpm release:check` to validate it. Verified with `pnpm release:check`, `cargo check --manifest-path cli/Cargo.toml --locked`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
17. [x] Fresh install on Node 26 printed a `url.parse()` deprecation warning from the pinned `pnpm@9.15.0`, making the cold-clone setup noisy before tests even ran. Updated the pinned package manager to `pnpm@10.24.0`. Verified with `pnpm --version`, `pnpm install --frozen-lockfile`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
18. [x] Fresh install under `pnpm@10.24.0` still warned about ignored build scripts because the repo used the old `neverBuiltDependencies` setting instead of explicit pnpm 10 build-script policy. Switched to `ignoredBuiltDependencies` for `sharp` and `onlyBuiltDependencies` for `esbuild` and `workerd`. Verified in a disposable clean install, then ran `pnpm install --frozen-lockfile`, `pnpm ci:full`, and `pnpm --filter @shrd/api test:integration`.
19. [x] D1 support helpers accepted SQL probe queries but ignored them, leaving dead probe SQL and a misleading `supportsD1Feature` abstraction in core API storage/idempotency/metrics code. Removed the fake probe/cache layer and kept direct D1 availability checks with existing legacy fallback behavior. Verified with stale-symbol `rg`, `pnpm --filter @shrd/api typecheck`, `pnpm --filter @shrd/api test:unit`, `pnpm --filter @shrd/api test:compat`, `pnpm --filter @shrd/api test:integration`, and `pnpm ci:full`. Worker dry-run moved from 152.13 KiB / gzip 33.00 KiB to 150.59 KiB / gzip 32.67 KiB.
